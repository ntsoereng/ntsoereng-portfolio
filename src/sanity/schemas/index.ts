import experience from './experience-schema';
import page from './page-schema';
import profile from './profile-schema';
import project from './project-schema';

export const schemaTypes = [profile, page, experience, project];
